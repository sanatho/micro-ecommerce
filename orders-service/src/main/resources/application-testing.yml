spring:
  application:
    name: orders-service
  datasource:
    url: jdbc:h2:mem:test
    driverClassName: org.h2.Driver
    username: sa
    password: password
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  jackson:
    serialization:
      FAIL_ON_EMPTY_BEANS: false
  flyway:
    enabled: true
server:
  port: 8083

eureka:
  client:
    enabled: false

keycloak:
  enabled: true

